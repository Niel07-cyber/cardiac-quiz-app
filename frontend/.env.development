VITE_BACKEND_URL=http://localhost:5000
# VITE_BACKEND_URL=https://cardiac-quiz-app.onrender.com 
# the backend is hosted an because of hosting purposes i put the 
#images in backend so if youre running locally use the top but if you 
#want to see this uncomment this and comment the top
